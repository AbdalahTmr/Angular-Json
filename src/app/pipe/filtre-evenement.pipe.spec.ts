import { FiltreEvenementPipe } from './filtre-evenement.pipe';

describe('FiltreEvenementPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreEvenementPipe();
    expect(pipe).toBeTruthy();
  });
});
